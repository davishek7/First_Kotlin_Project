fun main(){

    /*
    var str:String=null
    this line will give error because Kotlin doesn't support null assignment
    but we can assign null explicitly
    */
    var str:String?=null
    println(str)
    println(str?.length)

}